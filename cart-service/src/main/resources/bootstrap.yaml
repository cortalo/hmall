spring:
  application:
    name: cart-service # microservice name
  profiles:
    active: local
  cloud:
    nacos:
      server-addr: 192.168.1.6:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml
          - data-id: shared-log.yaml
          - data-id: shared-swagger.yaml
